## Smoke Check Report
**Date**: 2026-05-08
**Version**: v0.0.8
**Sprint**: Sprint 5 - monetization + daily challenge
**Engine**: WeChat Mini Game native Canvas 2D
**Checked by**: Codex smoke-check workflow

---

### Verdict: PASS

v0.0.8 is locally complete and ready to move to v0.0.9 polish. Real WeChat ad-unit IDs and cloud deployment remain release-environment checks, not local implementation blockers.

---

### Automated Tests

**Status**: PASS

Commands executed:

```bash
node tests/unit/core.test.mjs
node _smoke_test.js
node _smoke_test2.js
node -e "const { bootstrap } = require('./game.js'); ... startDailyChallenge() ..."
```

Results:
- `tests/unit/core.test.mjs`: all assertions passed.
- `_smoke_test.js`: 12 modules OK, 0 failed.
- `_smoke_test2.js`: all end-to-end checks passed.
- Daily challenge bootstrap smoke: generated today's challenge with deterministic seed `daily-2026-05-08-1454627708` and level id `8508`.

---

### v0.0.8 Scope Verification

| Scope Item | Evidence | Status |
|---|---|---|
| Rewarded video for items / revive / double score | `src/monetization/AdManager.js`, unit coverage with mock and wx-style ad API | PASS |
| Interstitial every 5 levels | `AdManager.showInterstitialAfterLevel`, unit coverage for level 4 skip and level 5/10 show | PASS |
| Result banner ads | `AdManager.showResultBanner`, unit coverage with mock and wx-style banner API | PASS |
| Ad fill monitoring | `AdManager.recordFill` / `getFillStats`, asserted for rewarded/interstitial/banner | PASS |
| Daily challenge seed | `src/social/DailyChallenge.js`, deterministic local/cloud seed coverage | PASS |
| Daily challenge cloud function | `cloud/functions/dailyChallenge/index.js`, local cloud-function smoke coverage | PASS |
| National daily leaderboard | `DailyChallenge.getNationalLeaderboard`, cloud-function rank smoke coverage | PASS |
| Item acquisition economy | `src/monetization/ItemManager.js`, free grant/ad grant/daily reward coverage | PASS |
| Null-safe economy calls | Fixed `applyLevelGrants(3, null)` crash and covered in tests | PASS |

---

### Manual / External Release Checks

These require a real WeChat project environment and cannot be executed from the local Node smoke harness:

- Replace placeholder ad-unit IDs with real WeChat ad-unit IDs before release validation.
- Deploy `cloud/functions/dailyChallenge` in WeChat Cloud Development and verify production database permissions.
- Run one real-device pass for ad pacing, rewarded-close behavior, banner placement, and daily reward UX.

These checks are tracked as release-environment validation for v0.1.0 and do not block v0.0.8 local completion.

---

### Completion Notes

- Fixed the only failing v0.0.8 smoke defect: `ItemManager.applyLevelGrants` now tolerates null option payloads.
- Added automated coverage for wx-style rewarded/interstitial/banner ad APIs.
- Added automated coverage for daily challenge cloud function fallback/ranking behavior.
- Confirmed previous smoke coverage still passes after the fix.
