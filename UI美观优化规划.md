# UI美观优化规划与执行清单

> 项目：`因果律消除 / Causal Chain Elimination`
>
> 当前阶段：`v0.0.8 开发期 UI 规划整理中`
>
> 规划版本：`v0.0.3-ui-plan`
>
> 更新时间：2026-05-08

> 说明：本文件为 UI 美观优化规划，持续记录任务拆分、执行顺序、风险控制与验收状态。
>
> 维护原则：本文件不再只做可行性评估，而是作为**可执行、可验收、可回退**的 UI 规划总表持续更新。

---

## 1. 规划目标

本文件用于定义当前项目在**界面美观度、视觉统一性、信息层级、交互质感**方面的具体推进路线，并将所有 UI 优化拆成**可执行任务**。

当前阶段的目标不是重构界面，而是：

- 在不影响玩法开发的前提下提升成品感
- 统一游戏内各个 UI 模块的视觉语言
- 为后续菜单、关卡、分享、排行榜等功能预留一致的设计基线
- 让界面从“能用”过渡到“可展示、可测试、可迭代”
- 把每一次 UI 改动变成可验收、可回退的最小单元

---

## 2. 当前开发进度与 UI 约束

### 2.1 已具备的开发基础

从现有代码和文档看，项目已经具备如下基础：

- 核心玩法框架已搭建
  - `CausalEngine`
  - `BoardGenerator`
  - `BoardValidator`
  - `Solver`
- 基础 UI 模块已拆分
  - `HUD`
  - `Toolbar`
  - `Tutorial`
  - `MenuManager`
  - `ResultPanel`
- 渲染与输入链路已存在
  - `BoardRenderer`
  - `TileAnimator`
  - `EffectRenderer`
  - `TouchHandler`
- 数据驱动内容已开始接入
  - `levels.json`
  - `tutorials.json`
  - `strings.json`
  - `themes.json`

### 2.2 当前阶段判断

结合版本路线图，项目已经从 **v0.0.7 社交裂变收口** 进入 **v0.0.8 完整变现与每日挑战** 的开发阶段。

当前更接近：

- **核心玩法与基础 UI 收口完成期**
- **变现与日常运营功能推进期**
- **中后期视觉体系统一和细节补完期**

也就是说，UI 现在仍然适合做“轻量升级”，但后续需要和 v0.0.8 的广告、每日挑战、奖励节奏一起规划，避免视觉工作和功能节奏脱节。

### 2.3 UI 变更边界

本规划明确以下边界，任何 UI 优化都应遵守：

- **允许**：颜色、阴影、圆角、字体、间距、层级、装饰、状态反馈、轻量动效
- **允许**：局部布局微调，只要不改变交互路径和触控命中逻辑
- **禁止**：修改核心玩法规则
- **禁止**：更改关卡逻辑、棋盘生成逻辑、消除逻辑、回溯逻辑
- **禁止**：把视觉优化包装成功能重构

### 2.4 失误预判与保底措施

为了避免 UI 优化误伤功能性，后续每次改动都默认执行以下保底动作：

- 先读现有文件，再做单点修改
- 每次仅改一个视觉模块或一个视觉点
- 不改事件绑定、不改命中区域、不改状态机
- 优先使用局部样式和绘制参数调整
- 每次修改后立即做语法与 linter 检查
- 若出现显示异常，优先回退到上一个稳定版本

---

## 3. UI 改造范围与原则

### 3.1 HUD 顶部状态栏

当前 `HUD` 已能展示：

- 关卡编号
- 状态
- 步数
- 最少步数
- 时间
- 回溯次数
- 暂停按钮

#### 主要问题

- 视觉上偏“工具条”，游戏感不够强
- 信息密度偏高，但层级不够明确
- 卡片、标题、按钮风格尚未统一
- 背景与文字虽然可读，但缺少精致的层次感

#### 可行优化

- 采用统一的半透明卡片背景
- 将标题区和状态区分层
- 统计项使用更轻的卡片样式
- 暂停按钮改为独立、统一风格的图标按钮

---

### 3.2 Toolbar 底部道具栏

当前 `Toolbar` 已支持：

- 道具展示
- 数量显示
- 限制提示
- 激励广告提示
- 选中态

#### 主要问题

- 图标表达较弱，偏功能占位符
- 底部栏整体感还不够强
- 道具卡片之间的视觉关系较松散
- 文案与数量提示层级可再优化

#### 可行优化

- 统一为“底部浮层卡槽”风格
- 道具按钮使用更圆润的卡片容器
- 图标、名称、数量三层分明
- 选中态加入轻微发光或描边
- 激励广告提示改为更轻量的标签样式

---

### 3.3 ResultPanel 结果面板

当前 `ResultPanel` 已支持：

- 胜利 / 失败两种模式
- 星级显示
- 步数 / 时间 / 回溯统计
- 按钮操作

#### 主要问题

- 面板结构清晰，但“奖励感”不足
- 胜利与失败的视觉反差可以更强
- 按钮样式还不够统一
- 星级呈现略偏基础，成就感不够明显

#### 可行优化

- 胜利使用偏金蓝配色，失败使用偏暗红配色
- 标题区做更明显的情绪表达
- 按钮统一为主按钮 + 次按钮 + 危险按钮
- 星级视觉升级为更像奖励徽章的表达

---

### 3.4 棋盘区域与背景

当前项目已有背景绘制和棋盘渲染能力，但整体观感仍偏“功能展示”。

#### 可行优化

- 背景加入轻微渐变与层次
- 棋盘区域与外围区域做清晰分隔
- 方块状态效果统一：默认、选中、匹配、失败、回溯
- 通过阴影、边缘高光、发光边改善材质感

---

## 4. 最小改动、最大提升的优化策略

本项目 UI 优化建议采用以下原则：

### 4.1 不改结构，只改表现

优先不调整：

- 页面布局结构
- 组件拆分方式
- 核心交互流程
- 渲染调用链

优先调整：

- 颜色
- 字体
- 间距
- 圆角
- 阴影
- 描边
- 按钮层级
- 状态反馈

### 4.2 优先升级三个关键模块

按性价比排序：

1. `HUD`
2. `Toolbar`
3. `ResultPanel`

原因：

- 覆盖最常见的核心界面
- 修改成本低
- 对用户感知最直接
- 不会干扰核心玩法验证

### 4.3 统一视觉语言

建议统一成一套轻量风格：

- 深色科幻底
- 半透明玻璃卡片
- 冰蓝主色
- 金色奖励强调
- 红色失败强调
- 高对比白字

这套风格和“因果、时间回溯、链式关系”的题材相符，也比较适合微信小游戏场景。

---

## 5. 可行性评估

### 5.1 视觉可行性：高

原因：

- 当前 UI 已经有明确组件边界
- 现有布局无需大改即可升级
- Canvas 2D 足以支撑所需的卡片、渐变、描边、阴影

结论：**高可行**

---

### 5.2 开发成本可行性：高

原因：

- 主要是样式层改造
- 不涉及核心算法改写
- 不需要引入新框架
- 修改集中在少数 UI 文件

结论：**低到中等成本，适合当前阶段**

---

### 5.3 风险可行性：高

主要风险：

- 改动过多导致当前可运行状态受影响
- 视觉增强过度，影响性能或可读性
- 各组件风格不统一，导致“局部变美、整体割裂”

规避策略：

- 先统一颜色与字体体系
- 再逐个优化卡片和按钮
- 每次只改一个模块并验证
- 保持现有布局不变

结论：**风险可控**

---

## 6. 推荐实施顺序

### Phase A：基础视觉统一

优先完成：

- 全局配色整理
- 字体层级统一
- 圆角与阴影标准化
- 标题、按钮、提示的风格统一

### Phase B：核心 UI 精修

优先完成：

- `HUD` 顶部卡片化
- `Toolbar` 底部浮层化
- `ResultPanel` 奖励感增强

### Phase C：局部氛围增强

在不影响性能的前提下补充：

- 成功高光
- 轻微入场动画
- 选中态发光
- 面板淡入上浮

### Phase D：棋盘与背景质感补完

在核心 UI 稳定后，再做：

- 背景层次优化
- 棋盘容器增强
- 方块默认态和选中态细化
- 回溯和消除视觉统一

#### 当前进度
- [x] 棋盘容器双层质感增强
- [x] 网格卡片与内层底板分层
- [x] 方块高光与边缘层次增强
- [x] 背景层次进一步优化
- [x] 回溯波纹视觉统一再打磨

---

## 7. 当前结论

### 结论等级：**适合立即推进 UI 美观优化**

本项目当前不适合做大规模重构，但非常适合做一轮**低风险、高收益**的视觉升级。

### 建议执行方式

- 不改玩法逻辑
- 不动组件架构
- 先统一视觉系统
- 优先改 `HUD`、`Toolbar`、`ResultPanel`
- 每完成一个模块就验证一次显示效果

---

## 8. 后续更新规则

本报告将随项目进度持续维护。建议每次完成以下任一里程碑后更新一次：

- 核心玩法完成
- 关卡系统完成
- UI 模块完成
- 道具系统接入
- 结果页完成
- 社交分享功能完成
- 排行榜完成
- 上线前总体验收完成

### 建议更新内容

每次更新时补充：

- 当前开发版本
- 已完成 UI 模块
- 新增可优化点
- 是否需要调整视觉基调
- 是否可以进入下一阶段

---

## 9. 可执行 UI 改造清单（标准状态表）

> 目标：把“美观优化”拆成可逐项落地、可验收、可回退的小任务。
>
> 原则：一次只推进一个小改动组，保证每一步都能在微信开发者工具里快速确认效果。

### 9.0 标准状态表

| 模块 | 状态 | 说明 |
|------|------|------|
| 全局视觉规范 | 进行中 | 统一色板、字号、圆角、阴影、按钮状态 |
| `HUD` 顶部状态栏 | 已完成 | 已完成视觉精修，保持稳定 |
| `Toolbar` 底部道具栏 | 已完成 | 已完成视觉精修，保持稳定 |
| `ResultPanel` 结果面板 | 已完成 | 已完成视觉精修，保持稳定 |
| 棋盘与背景质感补完 | 已完成 | 背景、容器、方块、回溯波纹已完成 |
| v0.0.8 变现/每日挑战相关 UI | 进行中 | 广告、每日挑战、奖励弹窗、库存反馈、结果面板文案、入口卡片、广告状态提示 |
| v0.0.9 世界主题 UI | 进行中 | 星夜 / 海洋等主题的视觉整合，当前推进星夜主题与全局微动画同步 |
| v0.1.0 发布前统一打磨 | 待做 | 全局一致性、低端机适配、边缘问题修复 |

### 9.1 全局视觉规范（先做一次，后续复用）

- [ ] 定义统一的主色 / 辅色 / 强调色 / 失败色
- [ ] 统一深色背景与卡片透明度基准
- [ ] 统一字体层级
  - [ ] 标题字号
  - [ ] 主信息字号
  - [ ] 辅助说明字号
  - [ ] 角标字号
- [ ] 统一圆角等级
  - [ ] 大卡片圆角
  - [ ] 小按钮圆角
  - [ ] 标签圆角
- [ ] 统一阴影与描边风格
- [ ] 统一按钮状态
  - [ ] 默认态
  - [ ] 按下态
  - [ ] 禁用态
  - [ ] 选中态

### 9.2 `HUD` 顶部状态栏改造

- [ ] 顶部背景改为半透明浮层卡片
- [ ] 左侧标题与状态信息重新分组
- [ ] 右侧暂停按钮改为统一图标按钮
- [ ] 统计项改为四个等宽小卡片
- [ ] 强调当前步数 / 最少步数的层级差异
- [ ] 时间与回溯次数的视觉权重降低
- [ ] 适配安全区顶部留白

**验收标准**
- 信息更清晰，但不增加阅读负担
- 顶栏与整体主题一致
- 不遮挡棋盘区域

### 9.3 `Toolbar` 底部道具栏改造

- [ ] 底部栏改为统一浮层底座
- [ ] 每个道具按钮改为一致尺寸的卡槽
- [ ] 图标、名称、数量三层信息重新排版
- [ ] 将“看广告+1”提示改为更轻量的辅助标签
- [ ] 加强选中态可视反馈
- [ ] 加强禁用态辨识度
- [ ] 与底部安全区对齐，避免贴边

**验收标准**
- 4 个道具按钮风格统一
- 用户能快速识别可用 / 不可用 / 需广告获取状态
- 不影响触控命中范围

### 9.4 `ResultPanel` 结果面板改造

- [ ] 胜利 / 失败两种状态使用差异化氛围色
- [ ] 标题区域增加情绪层级
- [ ] 星级展示方式升级为更有奖励感的样式
- [ ] 统计信息的对齐与间距统一
- [ ] 三个按钮统一风格并明确主次
- [ ] 失败态增加更清晰的重试引导
- [ ] 胜利态强化分享与下一关引导

**验收标准**
- 胜利时有明显奖励感
- 失败时有明确的继续挑战动机
- 按钮层级清楚，误触风险低

### 9.5 棋盘与背景的轻量美化

- [ ] 背景补充轻微渐变层次
- [ ] 棋盘区域与外围背景做更明确分隔
- [ ] 方块默认态增加轻微高光或边缘层次
- [ ] 方块选中态增加发光或抬升反馈
- [ ] 消除成功与回溯动画保持统一视觉语言

**验收标准**
- 棋盘更突出，背景更安静
- 动效不过度抢眼
- 不影响核心可读性

### 9.6 推荐实施顺序

1. 统一全局视觉规范
2. 改 `HUD`
3. 改 `Toolbar`
4. 改 `ResultPanel`
5. 最后补棋盘与背景的轻量美化

### 9.7 每次改动后的检查项

- [ ] 微信开发者工具中实际查看界面效果
- [ ] 375 / 390 / 414 / 428 宽度下是否正常显示
- [ ] 重要信息是否被遮挡
- [ ] 文案是否过长导致换行异常
- [ ] 触控热区是否准确
- [ ] 深色背景下可读性是否足够
- [ ] 是否与已有模块风格冲突

### 9.8 后续版本导向的 UI 任务规划

#### v0.0.8 变现 / 每日挑战 UI

- [ ] 激励广告按钮风格统一
- [ ] 死局复活弹窗与失败面板视觉区分
- [ ] 每日挑战入口卡片化
- [ ] 每日任务奖励展示的完成态 / 未完成态
- [ ] 道具库存和奖励到账提示统一
- [ ] 广告成功 / 失败状态提示统一

#### v0.0.9 世界主题 UI

- [ ] 星夜主题整体配色规范
- [ ] 海洋主题整体配色规范
- [ ] 世界切换的背景与棋盘容器过渡
- [ ] 主题粒子与回溯波纹颜色同步
- [ ] 主题内结果面板氛围色同步

#### v0.1.0 发布前统一打磨

- [ ] 全局字体与按钮比例再校准
- [ ] 低端机显示边缘修正
- [ ] 特殊机型安全区适配
- [ ] 高密度信息页的留白优化
- [ ] 所有 UI 模块最终一致性检查

这条路线改动最小，收益最大，也最适合当前开发节奏。
